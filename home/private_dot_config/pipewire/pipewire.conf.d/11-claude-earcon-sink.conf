context.modules = [
  {
    name = libpipewire-module-loopback
    args = {
      audio.position = [ FL FR ]
      capture.props = {
        media.class       = "Audio/Sink"
        node.name         = "claude-earcon-sink"
        node.description  = "Claude Earcons"
      }
      playback.props = {
        node.name    = "claude-earcon-sink.output"
        node.passive = true
        audio.position = [ FL FR ]
        # task-513 followup 2026-05-17: removed hard-pin to Bluebass MAC,
        # matching 10-claude-voice-sink.conf. Earcons now follow system
        # default sink. (Referenced preset-switcher script
        # voice-audio-preset.sh was never written; comment was aspirational.)
      }
    }
  }
]
