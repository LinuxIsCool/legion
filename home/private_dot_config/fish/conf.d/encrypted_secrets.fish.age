## PLACEHOLDER — encrypt with age after setup:
##   age -e -r "age1..." -o encrypted_secrets.fish.age secrets.fish
##
## Original content (secrets.fish):
##   set -gx LETTA_BASE_URL "http://localhost:8283"
##   set -gx LETTA_API_KEY "local"
##   (plus GITHUB_PERSONAL_ACCESS_TOKEN in fish_variables)
##
## This file will be replaced with actual age-encrypted content
## once `age-keygen` has been run and the key is configured.
